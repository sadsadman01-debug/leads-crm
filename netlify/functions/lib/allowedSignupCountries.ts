/** Countries currently allowed to sign up (Request Access / Become an
 * Affiliate) — a simple, easily-editable allowlist. Add a country here to
 * open signups to it; no other logic needs to change. Kept in sync with
 * `src/lib/allowedSignupCountries.ts` (separate frontend/backend bundles). */
export const ALLOWED_SIGNUP_COUNTRIES = ['Bangladesh']
